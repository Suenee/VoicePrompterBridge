# Contributing

Keep VPBridge transport-focused, preserve RAM-only queue behavior, never terminate generic `node.exe`, keep runtime configuration/secrets out of Git, update CHANGELOG for user-visible changes, and test both VP→BC and BC→VP paths before merging.
