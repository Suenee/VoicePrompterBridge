# Security

VPBridge is currently designed for a trusted local network. Prefer Local only when all components run on one computer. All Interfaces requires an API key and should be used only on a trusted LAN. Current transport uses WS:// and is not encrypted. Never commit `config/vpbridge.json`. Do not expose VPBridge directly to the public Internet.
