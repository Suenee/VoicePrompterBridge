import fs from 'node:fs';
import path from 'node:path';
import type {MessageDirection,MessageStatus} from '../bridge/message';
import type {VPBridgeConfig} from '../config/config';
export class Logger {private readonly logFile:string;private cleanupTimer?:NodeJS.Timeout;constructor(private readonly config:VPBridgeConfig['logging']){const dir=path.resolve(process.cwd(),config.directory);fs.mkdirSync(dir,{recursive:true});this.logFile=path.join(dir,'vpbridge.log');if(this.config.enabled){this.cleanup();this.cleanupTimer=setInterval(()=>this.cleanup(),60000);this.cleanupTimer.unref()}}system(message:string):void{this.write(`${this.timestamp()}  SYSTEM  ${message}`)}message(id:number,direction:MessageDirection,status:MessageStatus,payload?:string):void{const arrow=direction==='VP_TO_BC'?'VP→BC':'BC→VP';const suffix=payload!==undefined?`  ${this.singleLine(payload)}`:'';this.write(`${this.timestamp()}  #${id}  ${arrow}  ${status}${suffix}`)}dispose():void{if(this.cleanupTimer){clearInterval(this.cleanupTimer);this.cleanupTimer=undefined}}private singleLine(value:string):string{return value.replace(/\r\n|\r|\n/g,' ')}private write(line:string):void{console.log(line);if(!this.config.enabled)return;fs.appendFileSync(this.logFile,`${line}\n`,'utf8')}private timestamp():string{const d=new Date();const pad=(n:number,width=2)=>String(n).padStart(width,'0');return `${pad(d.getDate())}.${pad(d.getMonth()+1)}.${d.getFullYear()} ${pad(d.getHours())}:${pad(d.getMinutes())}:${pad(d.getSeconds())}.${pad(d.getMilliseconds(),3)}`}private cleanup():void{if(!fs.existsSync(this.logFile))return;const cutoff=Date.now()-this.config.retentionMinutes*60000;const lines=fs.readFileSync(this.logFile,'utf8').split(/\r?\n/);const kept:string[]=[];for(const line of lines){if(!line)continue;const m=line.match(/^(\d{2})\.(\d{2})\.(\d{4}) (\d{2}):(\d{2}):(\d{2})\.(\d{3})/);if(!m)continue;const[,dd,mm,yyyy,hh,min,ss,ms]=m;const ts=new Date(Number(yyyy),Number(mm)-1,Number(dd),Number(hh),Number(min),Number(ss),Number(ms)).getTime();if(ts>=cutoff)kept.push(line)}fs.writeFileSync(this.logFile,kept.length?`${kept.join('\n')}\n`:'','utf8')}}
